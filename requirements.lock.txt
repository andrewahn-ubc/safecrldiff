# Reproducible runtime dependencies. Simulator packages are installed from the
# pinned OopsieVerse checkout by scripts/bootstrap.py. Development-only tools
# such as pytest and Ruff are installed separately and never allowed to trigger
# source builds during cluster bootstrap.
h5py==3.11.0
gymnasium==0.29.1
huggingface-hub==0.24.7
imageio[ffmpeg]==2.35.1
matplotlib==3.9.2
numpy==1.26.4
pandas==2.2.3
# Alliance exposes a 17.0.0+dummy.computecanada sentinel wheel whose missing
# pyarrow-noinstall dependency is intended for module-managed environments.
# Arbitrary exact equality excludes that local-version sentinel and selects the
# official CPython wheel from PyPI instead.
pyarrow===17.0.0
PyYAML==6.0.2
scikit-learn==1.5.2
torch==2.3.1
