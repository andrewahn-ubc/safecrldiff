# Reproducible runtime dependencies. Simulator packages are installed from the
# pinned OopsieVerse checkout by scripts/bootstrap.py. Development-only tools
# such as pytest and Ruff are installed separately and never allowed to trigger
# source builds during cluster bootstrap.
absl-py==2.5.0
etils[epath]==1.13.0
glfw==1.12.0
h5py==3.11.0
gymnasium==0.29.1
huggingface-hub==0.24.7
imageio[ffmpeg]==2.35.1
matplotlib==3.9.2
numpy==1.26.4
pandas==2.2.3
# PyArrow, OpenCV, MuJoCo, lxml, Numba, and LLVMlite are installed separately
# from direct, hash-verified public wheels by scripts/bootstrap.py. Narval's
# resolver either exposes dummy sentinels or rejects their manylinux tags.
PyOpenGL==3.1.10
PyYAML==6.0.2
scikit-learn==1.5.2
termcolor==3.3.0
torch==2.3.1
